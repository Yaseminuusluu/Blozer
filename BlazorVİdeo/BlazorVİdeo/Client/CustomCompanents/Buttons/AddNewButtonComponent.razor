@inherits BaseButtonComponent


<ButtonComponent ButtonText="@(ButtonText ?? "Add")"
                 Color="@(Color ?? "primary")"
                 Icon="@(Icon ?? "plus")"
                 OnClick="OnClick"
                 ToolTypeStr="@(ToolTypeStr ?? "Add")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />