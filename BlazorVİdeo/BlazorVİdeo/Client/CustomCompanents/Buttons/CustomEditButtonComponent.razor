@inherits BaseButtonComponent


<ButtonComponent ButtonText="@(ButtonText ?? "Edit")"
                 Color="@(Color ?? "secondary")"
                 Icon="@(Icon ?? "pencil")"
                 OnClick="OnClick"
                 ToolTypeStr="@(ToolTypeStr ?? "Edit")"
                 IsOutline="@IsOutline"
                 IsSmall="@IsSmall" />