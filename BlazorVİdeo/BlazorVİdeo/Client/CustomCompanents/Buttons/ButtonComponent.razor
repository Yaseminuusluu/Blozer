@inherits BaseButtonComponent


<button class="btn@(IsSmall ? " btn-sm":"") btn@(IsOutline ? "-outline":"")-@Color"
        type="submit"
        disabled="@IsBusy"
        onclick="@OnClick"
        title="@ToolTypeStr">
    <span class="oi oi-@Icon oi-oi"></span>

    @if (IsBusy)
    {
        <span class="spinner-border spinner-border-sm mr-1"></span>
    }

    @ButtonText
</button>

